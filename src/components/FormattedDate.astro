---
interface Props {
	date: Date;
}

const { date } = Astro.props;

// Format: Jan 20, 2024 - more literary feel
const formatted = date.toLocaleDateString('en-US', {
	year: 'numeric',
	month: 'short',
	day: 'numeric',
});
---

<time datetime={date.toISOString()}>{formatted}</time>
